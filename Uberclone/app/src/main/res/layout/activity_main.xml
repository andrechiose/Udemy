<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    android:id="@+id/activity_main"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.andre.uberclone.MainActivity">

    <ImageView
        android:id="@+id/logo"
        android:layout_width="wrap_content"
        android:layout_height="150dp"
        android:src="@drawable/uber_logo"
        android:layout_centerHorizontal="true"/>

    <Switch
        android:layout_below="@id/logo"
        android:id="@+id/switch_uber"
        android:layout_marginTop="45dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Rider"
        android:layout_marginTop="50dp"
        android:id="@+id/rider"
        android:layout_marginEnd="8dp"
        android:layout_below="@id/logo"
        android:layout_toLeftOf="@+id/switch_uber"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Driver"
        android:layout_marginTop="50dp"
        android:id="@+id/driver"
        android:layout_marginStart="8dp"
        android:layout_below="@id/logo"
        android:layout_toRightOf="@+id/switch_uber"/>

    <Button
        android:text="Get Started"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/switch_uber"
        android:layout_centerHorizontal="true"
        android:onClick="getStarted"
        android:layout_marginTop="30dp"
        android:id="@+id/button_uber"/>

</RelativeLayout>
